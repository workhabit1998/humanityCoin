import { ReactComponent as BackArrowIcon } from "../../Assets/Images/BackArrowIcon.svg";
// import style from "../../Layout/Header/style.module.scss";

export function CommonBackButton({ onlyText,handleBackClick }){
    return(
        <>
        {/* {
            onlyText ? 

            <h3 className={style.header__top_cursor} onClick={handleBackClick}>
            Back
          </h3>

          :

          <h3 className={style.header__top_cursor} onClick={handleBackClick}>
            <span className={style.header__top_title_iconBack}>
              <BackArrowIcon />
            </span>{" "}
            Back
          </h3>

        } */}
        </>
        
    )
}
